import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  boardConatiner: {
    borderWidth: 1,
    borderColor: "#000000"
  },
  boardRow: {
    flexDirection: "row"
  }
});

export default styles;
